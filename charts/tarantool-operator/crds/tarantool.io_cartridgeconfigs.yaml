---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: cartridgeconfigs.tarantool.io
spec:
  group: tarantool.io
  names:
    kind: CartridgeConfig
    listKind: CartridgeConfigList
    plural: cartridgeconfigs
    singular: cartridgeconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: 'CartridgeConfig is the Schema for the cartridgeconfigs API More
          info: https://www.tarantool.io/doc/latest/book/cartridge/cartridge_api/modules/cartridge.clusterwide-config/'
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CartridgeConfigSpec defines the desired state of CartridgeConfig.
            properties:
              data:
                description: Data contains the configuration data.
                type: string
            type: object
          status:
            description: CartridgeConfigStatus defines the observed state of CartridgeConfig.
            properties:
              phase:
                default: Pending
                description: Phase indicates current state of CartridgeConfig
                type: string
            required:
            - phase
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
